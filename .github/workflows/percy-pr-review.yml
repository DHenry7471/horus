name: Percy — PR Test Review

on:
  pull_request:
    paths:
      - 'tests/**'
      - '**/*.test.ts'
      - '**/*.spec.ts'

permissions:
  contents: read
  pull-requests: write

jobs:
  percy-review:
    name: Percy Test Review
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # Need full history to diff

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clone claude_agents and start MCP server
        run: |
          git clone https://github.com/DHenry7471/claude_agents.git /tmp/claude_agents
          cd /tmp/claude_agents
          npm ci
          npm start &
          echo "MCP_PID=$!" >> "$GITHUB_ENV"
          # Wait for server to be ready (up to 30s)
          for i in $(seq 1 15); do
            curl -sf "http://localhost:3000/health" && break || sleep 2
          done

      - name: Build diff payload
        id: diff
        run: |
          DIFF=$(git diff origin/${{ github.base_ref }}...HEAD -- 'tests/**' '**/*.test.ts' '**/*.spec.ts')
          # Escape for GitHub output (multiline)
          echo "diff<<EOF" >> "$GITHUB_OUTPUT"
          echo "$DIFF" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Run Percy agent
        id: percy
        run: |
          OUTPUT=$(tsx agents/run-agent.ts percy "${{ steps.diff.outputs.diff }}")
          echo "output<<EOF" >> "$GITHUB_OUTPUT"
          echo "$OUTPUT" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
        env:
          CLAUDE_AGENTS_MCP_URL: 'http://localhost:3000'
          CLAUDE_AGENTS_TIMEOUT: '180000'

      - name: Stop MCP agent server
        if: always()
        run: kill "$MCP_PID" 2>/dev/null || true

      - name: Post Percy review as PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const output = `${{ steps.percy.outputs.output }}`;
            const body = [
              '## 🔍 Percy — Test Change Review',
              '',
              output,
              '',
              `---`,
              `*Generated by [percy](https://github.com/DHenry7471/claude_agents) · run \`${context.runId}\`*`,
            ].join('\n');

            // Find and update an existing Percy comment, or create a new one
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.startsWith('## 🔍 Percy')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
